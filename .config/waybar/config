{
    "layer": "bottom",
    "position": "bottom",
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-right": [
        "sway/language",
        "clock",
        "group/other",
    ],

    "clock": {
        "interval": 1,
        "format": "{:%H:%M:%S}",
        "tooltip-format": "{:%Y-%m-%d %H:%M:%S}",
    },

    "custom/popup": { "tooltip": false, "format": "#" },

    "group/other": {
        "orientation": "inherit",
        "drawer": {
            "children-class": "other-child",
        },
        "modules": [
            "custom/popup",
            "pulseaudio",
            "battery",
            "tray",
        ]
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": " 0%",
        "on-click": "pavucontrol",
        "scroll-step": 5,
        "format-icons": {
            "default": ["", ""]
        }
    },

    "tray": {
        "spacing": 8
    }
}
